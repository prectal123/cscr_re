# FP(Fingerprint) 방법론 아이디어 버전 기록

Logit/Perplexity descriptor의 문제의식(PROGRESS.md 2번 섹션 참고)에서 출발해, 이를 대체/보완하려는 새 descriptor 구성 방식들을 버전별로 기록. 전부 아직 **구현 전, 아이디어 단계**.

---

## v1.1 — Lexical Fingerprint

**핵심 아이디어**: Logit descriptor와 Perplexity descriptor를 하나의 통일된 측정 대상으로 대체.
- 측정 대상: 프롬프트에 대한 첫 T개 생성 토큰의 분포
- 추정 방식은 접근 권한에 따라 다름:
  - 화이트박스(로짓 접근 가능): 1회 forward pass로 정확한 softmax 분포
  - 블랙박스(API 등): temperature>0로 N회 반복 샘플링 → 경험적 빈도 분포로 근사 (Monte Carlo)
- 핵심 통찰: "확률"은 접근 권한에 따라 있을 수도 없을 수도 있지만 "샘플"은 항상 뽑을 수 있음 → 측정 대상은 하나, 추정 전략만 접근권한별로 다름.

**나중에 드러난 한계 (v1.2를 만들게 된 계기)**: 여전히 "모델이 어떤 토큰/스타일을 쓰는가"라는 **통계적 습관**을 보는 방식이라, logit descriptor와 같은 계열의 문제(architecture 차이 같은 capability-무관 요인이 섞여 들어갈 수 있음, RSA 분석에서 flan-t5-xxl이 T5 architecture라는 이유만으로 outlier가 된 사례가 시사하는 바)를 완전히 벗어나지 못함 — capability를 "직접" 묻는 방식이 아님.

상세 배경/근거는 `PROGRESS.md` 2번 섹션 참고.

---

## v1.2 — (이름 미정) LLM 백본 기반 전문성 요약 임베딩

**핵심 아이디어**: 모델의 응답 스타일이 아니라, **"이 모델이 뭘 잘하는가"를 LLM에게 직접 요약시켜서** 그 요약 텍스트를 임베딩한 걸 FP로 쓴다.

**절차**:
1. Probe prompt 수십 개를 준비
2. 타겟 모델에 태워서 Query-Response pool 생성
3. 그 pool **전체를 한 번에** 간단한 LLM 백본에 넘겨서, 이 모델이 보이는 전문성/도메인 특징을 자연어로 간추리게 함 (모델별로 요약 호출은 **딱 1번** — 프롬프트마다 호출하는 게 아님)
4. 그 요약 텍스트를 문장 임베딩 모델(예: MiniLM)로 임베딩 → 이게 그 모델의 FP

**v1.1 대비 장점**:
- **Capability에 더 직접적으로 정렬됨**: 토큰 통계가 아니라 "무엇을 잘하는가"를 명시적으로 묻는 방식이라, v1.1보다 의도한 측정 대상(capability)에 더 가까움.
- **모델 종류에 안 묶임 — 일반화 가능성**: FP 구성이 "자연어로 특징을 요약할 수 있는 대상"이면 뭐든 적용 가능 → LLM뿐 아니라 **MLLM, 심지어 모델이 아닌 툴(계산기, 검색 API 등)**도 같은 방식으로 descriptor를 만들어서 **같은 latent space에 정렬**시킬 수 있음. Logit/Perplexity/Lexical FP는 전부 "언어모델이어야 계산 가능"하다는 전제가 있는데, 이건 그 전제를 벗어남 — CSCR의 "LLM 라우팅"이라는 틀 자체를 넘어서는 확장.

**비용**: 걱정했던 것보다 훨씬 저렴함 — 요약 호출이 **프롬프트당이 아니라 모델당 1회**라서, 지금 pool(7개 모델) 기준 총 7번의 LLM 호출 + 가벼운 임베딩이면 끝. 대규모 인프라 불필요.

**아직 결정 안 된 것 / 열린 질문**:
- 이름
- 요약용 LLM 백본으로 뭘 쓸지 (로컬에 이미 있는 모델 재사용 vs 별도 선정)
- "전문성 요약"이 실제로 capability의 좋은 proxy인지는 아직 검증 안 됨 — LLM judge 계열 특유의 편향(장황한 응답을 더 전문적이라고 착각하는 등, bartscore의 길이 confound와 유사한 위험)이 있을 수 있음. 다만 개별 응답의 "정답 여부"를 판단하는 게 아니라 여러 응답에 걸친 "패턴"을 요약하는 작업이라, 그 점에서는 reference 없이 정답을 추측해야 하는 LLM judge의 전형적인 위험(사실관계 판단 불가)보다는 상대적으로 안전할 수 있음 — 이것도 검증 필요.
- v1.1과 마찬가지로, "capability 정렬이 개선됐다"는 걸 bartscore 같은 noisy한 ground truth로 증명하려는 시도는 이번 라운드 스코프 밖으로 유보 (PROGRESS.md 논의 참고) — 우선은 방향성 제안 + (여력 되면) 소규모 프로토타입/정성적 사례 정도로 남겨둠.

---

## 다음 버전을 위한 메모
새 아이디어가 나오면 이 파일에 v1.3, v1.4... 형식으로 계속 추가. 각 버전은 (a) 핵심 아이디어, (b) 이전 버전 대비 뭘 개선하려는 건지, (c) 새로 생기는 리스크/열린 질문을 반드시 포함.
